name: adb_kit
description: Typed Dart wrapper around the Android Debug Bridge (adb) — devices, packages, input, screen, logcat, files, intents, dumpsys, scripting.
version: 0.1.1
homepage: https://github.com/sbrsubuvga/ADB
repository: https://github.com/sbrsubuvga/ADB
issue_tracker: https://github.com/sbrsubuvga/ADB/issues
topics:
  - adb
  - android
  - automation
  - testing
  - desktop

environment:
  sdk: ^3.4.0

dependencies:
  meta: ^1.12.0
  async: ^2.11.0
  path: ^1.9.0

dev_dependencies:
  test: ^1.25.0
  lints: ^4.0.0
